
## set up the database connection properties.
## specifying the JDBC driver, database URL, username, and password required to connect to the MySQL database.
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/full-stack-ecommerce?useSSL=false&useUnicode=yes&characterEncoding=UTF-8&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=ecommerceapp
spring.datasource.password=ecommerceapp

# Specifies the Hibernate dialect for MySQL 8, allowing To Hibernate to generate SQL optimized for MySQL 8 features.
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# This configuration sets the base path for Spring Data REST endpoints to /api.
#spring.data.rest.base-path=/api


#logging.level.root =warn
spring.main.banner-mode=off


okta.oauth2.client-id=0oan03bq65i0heZFB5d7
okta.oauth2.issuer=https://dev-76684086.okta.com/oauth2/default




########################
#                      #
# HTTPS configuration  #
#                      #
########################

# Server web port
server.port=8443

#QA Demo
#server.port=9898

# Enable HTTPS support (only accept HTTPS requests)
server.ssl.enabled=true

# Alias that identifies the key in the key store
server.ssl.key-alias=luv2code

# Keystore location
server.ssl.key-store=classpath:luv2code-keystore.p12

# Keystore password
server.ssl.key-store-password=secret

# Keystore format
server.ssl.key-store-type=PKCS12














