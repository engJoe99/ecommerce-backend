
## set up the database connection properties.
## specifying the JDBC driver, database URL, username, and password required to connect to the MySQL database.
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/full-stack-ecommerce?useSSL=false&useUnicode=yes&characterEncoding=UTF-8&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=ecommerceapp
spring.datasource.password=ecommerceapp

# Specifies the Hibernate dialect for MySQL 8, allowing  Hibernate to generate SQL optimized for MySQL 8 features.
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# This configuration sets the base path for Spring Data REST endpoints to /api.
#spring.data.rest.base-path=/api


#logging.level.root =warn
spring.main.banner-mode=off